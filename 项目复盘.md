# 途途相册（省份打卡）小程序 · 项目复盘

> 记录时间：2026-07-10（2026-07-17 补充世界模式踩坑与难点分布）
> 项目路径：`/Users/zhuangjin/Desktop/jinjin做的软件/途途相册小程序/Chinamap-miniprogram`（原 `travelmap-miniprogram/`，已整理至桌面）
> 复盘人：产品 + 开发（结对）
> 状态：全国层审核基线 v1.0.3（main）；世界模式 feature/world 开发中（离岛框定位、海报标题已调，踩坑经验已补）

---

## 一、项目是什么

一个微信小程序，用「一张中国地图 + 34 个省级行政区」做旅行打卡：

- **第一级·全国层**：一张中国地图，34 省为最小单元。选省 → 传照片 → 该省在地图上高亮 → 一键生成「全国地图海报」。
- **第二级·省内层**：点进某省 → 显示该省地图（按地级市/区划分）→ 选市传照片 → 生成「该省海报」（高亮去过的市）。

产品名演变：**足迹中国 → 省份打卡 → 途途相册**（三易其名，顶栏、页内大标题、小程序后台名三处同步修改）。

---

## 二、技术架构（数据驱动，单工程）

- **微信小程序 Canvas 2D** 手绘地图与海报，非 web-view。
- **紧凑地理数组格式**（关键设计）：`utils/geo/{adcode}.js` 每个省/地区一个文件，导出 `[{ id, name, d(SVG path), bbox, label }, ...]`。由原始 GeoJSON 经 **Douglas-Peucker 简化（容差 ≈ 0.015°，约 1.6km，手机尺度视觉无损）+ 2 位小数取整 + 全省统一 bbox 归一化到 1000×1000** 预计算生成，运行时不再解析 GeoJSON。
- **两级抽象**：`getMapContext()` 统一抽象 country / province 两层；`drawMap` / `hitTest` / `drawPoster` 复用同一套逻辑，靠 `getMapContext()` 切源。
- **单工程原则**：扩展靠「加数据 + 加一层逻辑」，**不为每个省建独立工程/小程序**。
- **包体积**：34 省紧凑数组合计 ≈ 0.75MB，主包总计 1.23MB（< 2MB 上限），无需分包。

---

## 三、开发历程（里程碑）

1. 全国层 MVP：34 省轮廓、选省高亮、传照片、生成海报。
2. 地图精细度打磨：海南、台湾重绘（见坑 1、3）。
3. 省内层（feature/province 分支）：海南省内主岛居中放大 + 离岛小图、台湾仿海南。
4. 照片不变形（坑 4）+ 上传正方形裁剪（坑 4）。
5. 海报旧轮廓残影修复（坑 5）。
6. 交互修复：地图点击选不中（坑 6）。
7. 文案/产品修正：顶栏更名（坑 7）、标题去「打卡」、顶部重复地名删除、地名全量显示无省略号。
8. 隐私合规（坑 8、9）→ 审核通过 → 待发布。

---

## 四、踩坑记录（重点 ⭐）

### 坑 1 · 海南/台湾地图白色漏洞（耗时最长，三轮才根治）

**现象**：海南、台湾地图渲染出大片白色块，像是「地图破了个洞」。

**排查过程（三轮，每轮都以为修好了，实测还有白）**：
- **第一轮**以为是 SVG path 的 ring 方向混乱（微信/Canvas 的 non-zero winding rule 对内外环方向敏感）。统一约定 outer=CW、inner=CCW，用 shoelace 公式判定方向后修正。**仍有白。**
- **第二轮**发现 OSM 源里有些 inner ring 落在 outer 外部（变成碎片多边形）。改为每个 region 只保留面积最大的一个 ring。**仍有白。**
- **第三轮（真正根因）**：用 OSM Overpass API 按 relation 拼环，对多数市县产生了**退化形状**——实测覆盖率只有 8%~19%（即画出来的多边形只覆盖了该行政区真实面积的零头，剩下的全是白）。这不是方向问题，是数据源拼环本身就坏了。

**解决**：回退到 **DataV 源 + Chaikin 曲线细分（×2）** 的方案，覆盖率恢复正常（如昌江 36%、琼中 56%、三亚 53%），白色消失。经验：**当一种数据源在底层就退化时，和它缠斗不如换源。**

---

### 坑 2 · 微信打包器「动态 require」静默失效

**现象**：运行时报错 `Cannot find module './geo/xxxx.js'`，部分省地图打不开，且**本地开发者工具能跑、真机/上传后却挂**。

**根因**：`geo.js` 里用 `require('./geo/' + id + '.js')` 动态拼接路径加载各省数据。微信打包器做静态分析，**无法解析动态拼接的 require**，于是把这些模块直接漏打进包，运行时自然找不到。

**解决**：`GEO_SOURCES` 改为**显式静态 require 映射**：

```js
const GEO_SOURCES = {
  '110000': require('./geo/110000.js'),
  '460000': require('./geo/460000.js'),
  // ...每个省一行
}
```

**经验**：微信小程序里的 `require` 必须是「编译期能确定的字面量」。任何 `'./' + var` 的拼法在打包阶段就会被静默丢弃，且只在真机暴露，本地预览发现不了——这类坑要尽早用真机验证。

---

### 坑 3 · 台湾(710000) 市县细分数据缺失

**现象**：DataV 的 `{adcode}_full` 接口对台湾返回 404，拿不到 22 个县市的细分轮廓。

**解决**：改用开源数据集 **click_that_hood**（经 jsDelivr 代理获取 GeoJSON），属性取 `name_traditional_chinese`，并补一份「罗马名 → 数字 adcode」映射，使 id 与全国体系一致。

---

### 坑 4 · 照片被拉伸变形 + 上传体验

**现象**：有些照片上传到地图后变了形（人变胖、圆变椭圆）。

**根因**：主视图省份照片用了 5 参数 `ctx.drawImage(img, dx, dy, dw, dh)`，把任意比例的源图硬拉进 `size×size` 正方形，**不保持宽高比**。正方形原图（截图/社媒方图）不变形，但手机直拍的 4:3、16:9、3:4 都会被压扁 → 所以「有的变形有的不变」。

**解决（双保险）**：
1. **Canvas 兜底**：把主视图那处改为项目已有的 `drawCoverImage`（9 参数 `drawImage(img, sx, sy, sw, sh, dx, dy, dw, dh)`），先按目标比例从原图居中裁一块正方形再画，等价于 CSS `object-fit: cover`，**永不被拉伸**。全工程 5 处画照片统一走此函数。
2. **上传时裁剪**：选完照片调微信原生 `wx.cropImage({ cropScale: '1:1' })`，弹出正方形选框让用户自己构图，存进来的一定是方图；并弹「拖动选框，裁剪为正方形」提示。老机型不支持裁剪时自动降级回原图，由上面的 canvas 兜底兜住。

---

### 坑 5 · 海报「旧地图轮廓」残影

**现象**：生成的海南/台湾海报里，主地图旁边浮着一团错位、像「旧版地图」的轮廓（含跨海的三沙、金門/澎湖/連江）。

**根因**：海报里有一块「地图轮廓投影」阴影。它**用了与主地图不同的变换**（省内主图是「fit 居中放大」，阴影却是另一套 uniform 变换）→ 阴影被画到错误位置；且它遍历了省份**所有**区划（含离岛/飞地）→ 跨海轮廓全被画进阴影。实测海南旧阴影错位数百像素、且含三沙跨海轮廓。

**解决**：抽出共用的 `getMapRenderPlan()`，让主地图和阴影**用同一套变换、同一批「主陆地」区域**，离岛照常画成右下角小图 inset。残影消失，同时保留柔和悬浮的立体感。

---

### 坑 6 · 地图点击选不中（点不动）

**现象**：点地图里的省份想选中上传照片，经常「点空」，尤其在页面往下滚过之后。

**根因（两个叠加）**：
1. **坐标算法受滚动影响**：旧逻辑 `e.detail.x - rect.left`，其中 `e.detail.x/y` 是相对整页文档坐标，而 `boundingClientRect().top` 是相对当前视口（已扣滚动），两者相减**只在未滚动时对**。页面可滚动，一滚就点哪偏哪。
2. **canvas 上 `bindtap` 本身飘**：微信 `<canvas>` 的 `bindtap` 触发不稳定，轻点常不触发。

**解决**：canvas 换成 `bindtouchstart/move/end`，直接用 `e.touches[0].x/y`（微信明确为相对 canvas 左上角坐标，不受滚动影响），并用「按下→抬起位移 < 10px 才算点击」区分点击与滑动，避免滑页面误选。

---

### 坑 7 · 顶栏名称被页面级配置覆盖

**现象**：`app.json` 全局已设 `navigationBarTitleText: "省份打卡"`，但小程序打开顶栏显示「足迹中国」。

**根因**：`pages/index/index.json` 的**页面级** `navigationBarTitleText` 写了「足迹中国」，页面级覆盖全局级。后产品定名「途途相册」，同步改了 `app.json` 全局、`index.json` 页面级、`index.wxml` 页内大标题、`project.config.json` 描述四处。

**经验**：微信小程序配置有「全局 → 页面 → 组件」的覆盖链，改标题要全局页面一起改，否则出现「代码里明明写了却没生效」。

---

### 坑 8 · 隐私合规拦截上传（`chooseMedia` 报错）

**现象**：点上传照片报 `chooseMedia:fail api scope is not declared in the privacy...`。

**根因**：`app.json` 开了 `__usePrivacyCheck__: true`，但既缺本地隐私声明文件，也没在微信后台声明 `chooseMedia` 接口。

**解决（上线必须的三步）**：
1. 本地加 `__privacy__.json` 声明用到的隐私接口（`chooseMedia`/`chooseImage`/昵称头像/相册写入）。
2. 代码接 `wx.onNeedPrivacyAuthorize`，弹官方隐私授权窗，点「同意」后 `resolve()` 放行被挂起的 `chooseMedia`。
3. **后台补全**：去 mp.weixin.qq.com「用户隐私保护指引」勾选并说明相册/摄像头/相册写入/昵称头像四个接口，审核通过。

**开发期临时方案**：为能本地测上传，曾把 `__usePrivacyCheck__` 临时改 `false`；**后台指引通过后必须改回 `true`**，否则线上用户仍卡在上传。（本次已完成切换回 true。）

---

### 坑 9 · 微信审核驳回：昵称/头像说明与代码不符

**现象**：隐私指引提交审核被驳回，原因「【收集你的昵称、头像接口】说明内容不符合接口使用场景」。

**根因**：后台填写的接口用途描述太笼统（如「用于用户身份识别」），与代码实际用途（仅用于画在海报右上角）对不上。微信审核是人工比对「你说用来干嘛 vs 代码里真在干嘛」。

**解决**：如实填写——「用于获取你的微信头像和昵称，展示在生成的旅行地图海报右上角区域」，与代码一一对应，重新提交即通过。**经验：隐私说明要「少写、写实、和代码 100% 对应」，多写反而容易被驳。**

---

### 坑 10 · geo 路径统计虚低（计数 bug）

**现象**：自查时地图点数显示只有真实值的一半。

**根因**：路径拼接用 `'M' + x`（字母与坐标间无空格），导致坐标与指令粘连，按简单正则解析时漏算。

**解决**：改用 `d.match(/[ML]/g)` 统计指令数，得到真实点数。属自查工具的小 bug，顺带记录。

---

### 坑 11 · 地图精细度与描边粗细反复拉锯

海南/台湾的视觉精细度、各市描边粗细、离岛小图（inset）尺寸与样式，与用户来回多轮：
- 用最小容差保原始点 → 仍粗糙 → 换 OSM 高精度源（6407 点）→ 因坑 1 白洞问题回退 DataV+Chaikin（4952 点）。
- 描边从粗到细多次微调（最终省内市界 bleed 0.3 / 高亮 0.35 / 普通 0.15，离岛岛屿 1.55/s）。
- inset 外框对齐全国地图南海诸岛样式（圆角 8、描边 1.0px），台湾离岛文字放框上方居中 10px、海南框内 11px。

**经验**：视觉类需求要用「可对比的预览图」沟通，纯文字描述「再细一点」很难对齐预期；本项目养成了「改完生成 SVG/HTML 预览对照」的习惯。

---

### 坑 12 · 全球模式图片内容安全（审核驳回，接云开发 imgSecCheck）

**现象**：小程序提交审核被驳回，原因「图片功能存在信息安全风险」。因为用户上传的照片属于 UGC 内容，未做内容安全检测。

**解决**：接入微信云开发。`app.js` 加 `wx.cloud.init({ env })`；`project.config.json` 开 `cloudfunctionRoot`；新建云函数 `cloudfunctions/imgSecCheck` 调 `openapi.security.imgSecCheck`；客户端上传前先调云函数检测，违规拦截并 toast。需：开通云环境（本项目 `cloud1-d3gp4b4kz2be22e9a`）、建任意集合激活环境、右键部署云函数、更新云调用权限（同步云函数列表 + 更新云调用权限两处绿勾）。

**经验**：带「用户上传图片」功能的小程序，内容安全检测是微信审核的**硬性前置**，早接比被驳回再补更省时间。

---

### 坑 13 · 全球模式两个渲染 Bug（fit 缩放溢出 + 大洲点不中）

**现象**：①切到全球 tab 点亚洲/欧洲/非洲，地图上无法选中/钻取；②进入非洲、南美下级国家视图时，地图有时渲染特别大、不完整，时好时坏。

**根因**：Bug1 初判 `onMapTap` 在全球模式只 `selectRegion` 未钻取，曾加自动钻取，但用户明确反对跳页（见坑 15），撤销；Bug2 真因是 `getMapRenderPlan` 的 fit 缩放**无防护**——部分洲（非洲 953×1000、南美 686×1000）国家级归一化坐标接近满幅，画布宽高比不匹配时 scale 偏大 → 溢出裁剪。

**解决**：全球层 + 省内层均加 `safeW/safeH` 防护（画布未就绪回退 viewbox）+ `maxScale` 上限 clamp，模拟验证 7 洲均 fit 不溢出。

**经验**：地图 fit 缩放**必须加 scale 上限 + 数据满幅防护**，否则部分区域偶发溢出，且「时好时坏」最难排查。

---

### 坑 14 · hitTest `inset is not defined` 崩溃

**现象**：重构 hitTest 主区域命中过滤时误用了未定义变量 `inset`，点地图即 `ReferenceError: inset is not defined`。

**解决**：改为从 `this._provinceInset` 遍历收集 `insetRegionIds`，过滤掉已在离岛补充框内的 region。

**经验**：重构命中检测别引入未定义变量；离岛框区域统一从 `_provinceInset` 收集 ID 过滤。

---

### 坑 15 · 全球离岛补充框（inset）定位无系统化，只能按大洲硬调（最反复）

**现象/过程**：离岛小框默认放右下角。但欧洲框挡主大陆、非洲框挡地图 → 先整体挪左上角（用户：只挪欧洲）→ 欧洲文字避让（框右侧→框上方→上移）→ 非洲挪左侧 → 左侧中间偏下 → 非洲文字挪框上方……一轮轮硬调。

**根因**：框位置是写死的「相对画布某角的魔法数字」（`area.x + margin` / `area.w - frameW - margin`），**没有「自动找地图空白」算法**；地图不规则、空白因洲而异 → 每加一个大洲都要重新调。

**解决（临时）**：按 `provinceId` 特判——欧洲左上角、非洲左侧中间偏下、其余右下角；文字分别置于框右侧 / 框正上方 / 框内。

**经验/待优化**：小元素布局应做「按地图像素占用自动选空象限」的系统化方案，避免逐个硬调。**这是整个项目里最反复、最耗交互轮次的点。**

---

### 坑 16 · 开发者工具打不开 / 卡编译 / `param projectpath is undefined`

**现象**：开发者工具卡编译转圈；强杀进程后重开报 `param projectpath is undefined`（长堆栈）。

**解决**：卡编译 → 清缓存/杀进程/删 `SingletonLock`/清 `WeappCache`+`WeappLocalData`+`Session`；`projectpath` 丢失 → 清 `Default/Sessions` 后手动重导项目。

**经验**：开发者工具有状态损坏先清会话缓存；项目**搬家后必须在开发者工具里重新打开**（路径变了旧会话会失效）。

---

### 坑 17 · 包体积逼近 2MB 红线（世界模式 173 国）

**现象**：加世界模式 173 国后主包达 ~1.8MB（<2MB 上限，但 >1.5MB 建议值）。

**经验**：扩展地理数据要持续盯主包体积；靠「紧凑数组预计算 + 原始 GeoJSON 不进包归档」控制。若后续再涨，需考虑分包或抽离到云存储按需拉取。

---

### 坑 18 · GitHub 推送 502 / 网络失败

**现象**：多次 `git push` 报 `502`、`HTTP2 framing error`、`CONNECT tunnel failed`，代码推不上去，只能留本地。

**解决**：用钥匙串取 token + Basic 鉴权推送（`Authorization: Basic $(printf 'x-access-token:%s' $TOKEN | base64)`）；网络恢复后重推。

**经验**：国内推 GitHub 常受网络/代理影响，**失败先查网络而非死循环重试**；代码本地先 `commit` 保安全，断网也不丢。

---

## 五、主动移除/调整的产品逻辑

- **计数代码全部移除**：原 header 计数器、海报「已点亮 N/M」、分享标题里的计数（countLit/litCount/totalRegions）全部删除。海报改列「去过的地方」地名（不含数字），地图点亮高亮与照片存储保留。
- **省份海报标题去「打卡」**：从「澳門打卡」改为纯城市名「澳門」。
- **顶部重复地名删除**：原「标题下方」和「海报底部」两处出现同样的地名清单，删掉标题下方那次，地名只在底部出现一处。
- **地名全量显示**：原 `getVisitedText()` 限前 6 个 + 省略号，改为整段铺满、全部显示，底部区域自动扩展、日期跟随下移不重叠。

---

## 六、上线与发布（待执行）

当前状态：功能完成、隐私合规通过、`__usePrivacyCheck__` 已恢复 `true`。

发布流程：
1. 开发者工具清缓存重编译 → 验证隐私弹窗 + 上传正常、顶栏「途途相册」。
2. 开发者工具顶部「上传」推新版本（建议版本号 1.0.4，填备注）。
3. mp.weixin.qq.com「版本管理 → 提交审核」。
4. 审核通过 → 发布。

**另需确认**：后台「小程序名称」是否已是「途途相册」（代码顶栏已改，但列表/搜索名在后台，代码动不了）。

---

## 七、经验总结（给后来者的 7 条）

1. **微信小程序里的 `require` 必须是字面量**，动态拼接会被静默漏打包，且只在真机暴露——地图/数据按省拆分时务必用静态 require 映射表。
2. **地图白洞先怀疑坐标变换/方向，再怀疑数据源**：当一种数据源在底层退化（覆盖率个位数）时，换源比和它缠斗更快。
3. **照片绘制统一走 `object-fit: cover` 语义**（9 参数居中裁方），并在上传入口就裁方，双保险永不变形。
4. **canvas 交互用 touch 事件拿相对画布坐标**，别用 `bindtap` + `detail.x - rect` 那套（滚动即失灵）。
5. **隐私合规是上线前置项，不是测试项**：本地可临时关检查，但发布前必须本地声明 + 代码接授权弹窗 + 后台指引审核通过三件套齐备。
6. **隐私接口说明要写实、和代码 100% 对应**，多写/虚写反而被人工审核驳回。
7. **视觉类需求用可对比预览图沟通**，避免「再细一点 / 再小一点」在文字里空转。
8. **UGC 图片必须接内容安全检测**（云开发 imgSecCheck），否则带上传功能的小程序必被审核驳回。
9. **地图 fit 缩放必须加 scale 上限 + 满幅防护**，否则部分区域偶发溢出且时好时坏。
10. **小元素布局别写死角落魔法数字**：不规则地图上应按「地图空白」系统化选位，避免按大洲逐个硬调（本项目最反复的点）。

---

## 八、纵观全项目的难点分布（2026-07-17 补充）

按出现频率与返工轮次，整个项目的难点可归为四类：

1. **地图数据：精度 vs 体积 vs 打包（基础性、反复出现）**——OSM 高精度源退化回退 DataV+Chaikin（坑 1）、主包 2MB 红线（坑 17）、微信动态 require 静默漏打包（坑 2）。每次扩展（加省、加世界）都会重新撞上。
2. **Canvas 渲染引擎：fit 缩放 + 命中检测（技术最深）**——溢出（坑 13）、hitTest 崩溃（坑 14）、双层抽象（country/province → China/world）。今天下午的离岛框反复（坑 15）本质是其下游症状。
3. **微信平台 / 审核 / 工具链（最频繁打断用户）**——开发者工具打不开（坑 16）、审核驳回逼接云安全（坑 8/9/12）、GitHub 推送失败（坑 18）。不写代码却总卡住节奏。
4. **双模式 UX 一致性（概念决策反复）**——点地图跳不跳（坑 15 相关）、选中 vs 钻取、各级海报标题。先定清楚规则代码才好写。

**一句话**：技术上最难是「地图数据 + 渲染引擎」的组合；对用户体验上最磨人的是「微信平台环境」。离岛框摆放只是渲染引擎的一个切面，不是全局最难。
